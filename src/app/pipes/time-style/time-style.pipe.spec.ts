import { TimeStylePipe } from './time-style.pipe';

describe('TimeStylePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeStylePipe();
    expect(pipe).toBeTruthy();
  });
});
